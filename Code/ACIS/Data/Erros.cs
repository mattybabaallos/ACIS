namespace Data
{
   public enum Erros
    {
        SUCCESS = 0,
        RECEIVED_FEWER_THAN_TWO_BYTES,
        INVALID_OPERATION,
        INVALID_DEVICE,
        COULD_NOT_PERFORM_OPERATION,
        COULD_NOT_DECODE_BYTES,
    }
}
