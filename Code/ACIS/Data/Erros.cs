namespace Data
{
   public enum Erros
    {
        SUCCESS = 0,
        RECEIVED_FEWER_THAN_TWO_BYTES = -1,
        INVALID_OPERATION = -2,
        INVALID_DEVICE = -3,
        COULD_NOT_PERFORM_OPERATION = -4,
        COULD_NOT_DECODE_BYTES = -5,
    }
}
